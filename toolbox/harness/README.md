# harness

このディレクトリはハーネス仕様の保管場所です。

## ディレクトリ

- `bin/`: ハーネス固有の補助実行ファイル
- `config/`: 実行設定
- `input/`: 入力サンプル
- `output/`: 出力先（生成物は原則コミットしない）

## 運用方針

- ハーネスの実行入口は `scripts/` 配下に置く。
- ここには入出力仕様と設定を置き、再現可能性を優先する。

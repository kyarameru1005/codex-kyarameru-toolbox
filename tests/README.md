# tests

このディレクトリは、自動検証コードの置き場です。

## 方針

- 実装や運用スクリプトの回帰を確認するテストを配置する。
- テストの補助データが必要な場合も、この配下または既存の適切な場所に限定する。
- 単発の調査メモは置かず、説明が必要ならこの `README.md` か `docs/` に残す。

## 現在の主な対象

- `tests/test_install.py`: 配備スクリプトの検証
- `tests/test_report_writer.py`: レポート生成系の検証
